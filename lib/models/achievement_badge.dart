import 'package:flutter/material.dart';

class AchievementBadge {
  String title;
  String description;
  String icon;
  Color color;

  AchievementBadge({
    required this.title,
    required this.description,
    required this.icon,
    required this.color,
  });
}
