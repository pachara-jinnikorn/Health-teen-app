name: health_teen
description: A health and wellness tracking app for teens
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # QR / Utilities
  qr_flutter: ^4.1.0

  # State Management
  provider: ^6.1.1
  url_launcher: ^6.2.1
  # Firebase
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.5.2
  
  # Local Storage
  shared_preferences: ^2.2.2
  
  # UI Components
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.1
  intl: ^0.18.1

  # Charts
  fl_chart: ^0.66.0

  # Icons
  cupertino_icons: ^1.0.2

# ✅ SINGLE dev_dependencies section (merged both sections)
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  
  # Testing packages
  integration_test:
    sdk: flutter
  mockito: ^5.4.4
  build_runner: ^2.4.13
  fake_cloud_firestore: ^3.0.3
  firebase_auth_mocks: ^0.14.1
  test: ^1.25.8

flutter:
  uses-material-design: true